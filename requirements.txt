# Paper-4-1 shared analysis code — Python dependencies
# Tested with Python 3.12.12 on Linux x86_64 (glibc 2.34).

# Core scientific stack (versions used to produce the submitted figures)
numpy==2.3.5
pandas==3.0.3
scipy==1.17.1
xarray==2026.4.0
netCDF4==1.7.4            # xarray engine for .nc I/O

# Geospatial
geopandas==1.1.3          # shapefile I/O (replaces fiona, which is not required here)
shapely==2.1.2            # vectorised point-in-polygon for the China-land mask
pyshp==3.0.9              # shapefile reader for cartopy map geometries

# Plotting / maps
matplotlib==3.10.9
cartopy==0.25.0
tabulate==0.10.0          # markdown rendering of the result tables

# --- optional: only needed by make_fig_doc_pdf.py (figure-data reference PDFs) ---
# weasyprint==69.0        # also requires system fonts, e.g. Noto Sans CJK SC
